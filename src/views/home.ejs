<%- include('partials/header') -%>

    <h1>Home</h1>

    <p>
        <%= StartingContent %>
    </p>

    <div>
        <% posts.forEach((element)=> { %>
             <h1><%=element.title %></h1> 
             <p> <%=element.story.substring(0,100)+"..."%><a href= "/posts/<%=element.title%>">Read more</a></p> 
        <% }) %>
    </div>

<%- include('partials/footer') -%>